import { FC } from 'react';

import { IconCategory } from '../impl/IconCategory';
import { IconProps } from '../impl/types';

/**
 * Declaration used by template file
 * Please note that only *path* is taken to production code when generating actual icon file
 */
export declare const createIcon: (
  path: string,
  category: IconCategory,
  keywords?: Array<string>,
) => FC<IconProps>;
