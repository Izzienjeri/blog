// istanbul ignore file - this is a module used only for development version of icon

export const iconCategories = [
  'general',
  'social',
  'application',
  'permissions',
  'dam',
  'cloudinary',
] as const;

export type IconCategory = (typeof iconCategories)[number];
