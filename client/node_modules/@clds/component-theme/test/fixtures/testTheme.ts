import { merge } from 'lodash';

import { createTheme } from '../../src/api/createTheme';

const primary = {
  background: {
    color: 'palette.primary',
    contrast: 'palette.contrastInvert',
  },
  focus: {
    color: 'palette.primary',
  },
  border: {
    radius: 'radius.lg',
  },
  font: {
    color: 'palette.contrastInvert',
  },
};

const secondary = {
  background: {
    color: 'palette.secondary',
    contrast: 'palette.contrastHigh',
  },
  focus: {
    color: 'palette.primary',
  },
  border: {
    radius: 'radius.lg',
  },
  font: {
    color: 'palette.contrastHigh',
  },
};

const error = {
  background: {
    color: 'palette.error',
    contrast: 'palette.contrastInvert',
  },
  focus: {
    color: 'palette.primary',
  },
  border: {
    radius: 'radius.lg',
  },
  font: {
    color: 'palette.contrastInvert',
  },
};

const link = {
  background: {
    color: 'palette.primary',
  },
  font: {
    color: 'palette.primary',
  },
  border: {
    radius: 'radius.lg',
  },
  focus: {
    color: 'palette.primary',
  },
};

const menu = {
  background: {
    color: 'palette.contrastHigh',
  },
  font: {
    color: 'palette.contrastLow',
  },
  border: {
    radius: 'radius.lg',
  },
  focus: {
    color: 'palette.primary',
  },
};

const pillTheme = { border: { radius: '1000px' } };

// sourced from button component theme
export const themeDefinition = {
  pill: {
    primary: merge({}, primary, pillTheme),
    secondary: merge({}, secondary, pillTheme),
    error: merge({}, error, pillTheme),
    link: merge({}, link, pillTheme),
    menu: merge({}, menu, pillTheme),
  },
  solid: {
    primary,
    secondary,
    error,
    link,
    menu,
  },
  group: {
    primary: {
      divider: {
        color: 'palette.surface',
        baseColor: 'palette.primary',
      },
    },
    secondary: {
      divider: {
        color: 'palette.contrastHigh',
        baseColor: 'palette.secondary',
      },
    },
  },
  split: {
    primary: {
      open: {
        accentColor: 'palette.backgroundAlt',
      },
    },
    secondary: {
      open: {
        accentColor: 'palette.contrastHigh',
      },
    },
  },
};
export const theme = createTheme(themeDefinition, 'test-component');
