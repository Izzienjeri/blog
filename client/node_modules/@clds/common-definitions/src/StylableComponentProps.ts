/** Interface for props of components that can be wrapped with styled() from styled-components. */
export interface StylableComponentProps {
  /** Class name to apply to root node (enables wrapping component as styled). */
  className?: string;
}
