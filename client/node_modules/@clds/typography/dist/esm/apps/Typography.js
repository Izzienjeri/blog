import styled from 'styled-components';
import './theme';
import { typographyMixin } from './typographyMixin';
/** `Typography` is responsible for styling the text according to the designs */
export const Typography = styled.div.attrs((props) => {
    var _a;
    return ({
        'data-test': (_a = props['data-test']) !== null && _a !== void 0 ? _a : 'typography',
    });
}) `
  ${typographyMixin};
`;
Typography.defaultProps = {
    size: 'sm',
    type: 'regular',
};
//# sourceMappingURL=Typography.js.map