import { css } from 'styled-components';
import { websiteTheme } from './websiteTheme';
export const websiteTypographyMixin = (kind, size, type) => {
    const kindBranch = websiteTheme[kind];
    if (!(size in kindBranch)) {
        throw new Error(`Website typography: ${size} size is not supported for website/${kind} typography`);
    }
    const sizeBranch = kindBranch[size];
    if (!(type in sizeBranch)) {
        throw new Error(`Website typography: ${type} type is not supported for website/${kind}/${size} typography`);
    }
    const typeBranch = sizeBranch[type];
    return css `
    font-family: ${kindBranch.fontFamily};
    font-size: ${sizeBranch.fontSize};
    line-height: ${sizeBranch.lineHeight};
    color: ${typeBranch.color};
    font-weight: ${typeBranch.fontWeight};
  `;
};
//# sourceMappingURL=websiteTypographyMixin.js.map