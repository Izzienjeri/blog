import { GlobalTheme } from '@clds/component-theme';
export declare type Size = keyof GlobalTheme['spacing'];
export interface FoundationContext {
    /** the declared expected space that consuming component can allocate */
    size: Size;
}
export declare type Tone = 'solid' | 'subtle';
export declare type Variant = 'primary' | 'secondary' | 'error' | 'success' | 'warning' | 'overlay';
export interface SurfaceContext {
    variant: Variant;
    tone: Tone;
}
